language: php

sudo: false

php:
  - 5.5
  - 5.6
  - hhvm
  - 7.0

env:
  - LARAVEL_VERSION="~5"

matrix:
  allow_failures:
    - php: hhvm
    - php: 7.0

script:
  - vendor/bin/phpspec run
  - vendor/bin/phpcs --standard=PSR2 src
  